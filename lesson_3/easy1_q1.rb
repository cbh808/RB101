numbers = [1, 2, 2, 3]
numbers.uniq          # does not mutate numbers

puts numbers          # prints numbers as strings, each on new line
puts numbers.inspect  # inspects numbers
p numbers             # p inspects numbers
p numbers.uniq        # prints uniq array